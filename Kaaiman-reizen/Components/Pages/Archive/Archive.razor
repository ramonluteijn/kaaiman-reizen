@page "/archive"
@using Kaaiman_reizen.Data.Entities
@using Microsoft.AspNetCore.Authorization
@attribute [Authorize(Roles = "Planner")]
@rendermode InteractiveServer

<PageTitle>Archief</PageTitle>

<h1 class="h4 d-flex align-items-center gap-2">Archief
    <Tooltip>
        <strong>Archief — hoe werkt dit?</strong>
        <ul>
            <li>Gebruik de zoekbalk om op naam van de planning te zoeken.</li>
            <li>Klik op een kolomkop om te sorteren (Naam, Aanmaakdatum, Planning Jaar).</li>
            <li>Klik op een rij om de gearchiveerde planning te openen en de details te bekijken.</li>
        </ul>
    </Tooltip>
</h1>

<div class="card">
    <div class="card-body">
        <div class="d-flex justify-content-between align-items-center mb-3">
            <input class="form-control w-25"
                   placeholder="Zoek op naam..."
                   @bind="_searchTerm"
                   @bind:event="oninput" />
        </div>
            
        <Table TItem="PlanningVersion" Items="FilteredPlans" Path="plans" DetailHrefSelector="@GetDetailHref">
            <HeaderContent>
                <th @onclick='() => SortBy("Name")'>
                    Naam @(GetSortIcon("Name"))
                </th>
                <th @onclick='() => SortBy("CreatedAt")'>
                    Aanmaak Datum @(GetSortIcon("CreatedAt"))
                </th>
                <th @onclick='() => SortBy("PlanningYear")'>
                    Planning Jaar @(GetSortIcon("PlanningYear"))
                </th>
            </HeaderContent>
            <RowTemplate>
                <td>@context.Name</td>
                <td>@context.CreatedAt</td>
                <td>@context.PlanningYear</td>
            </RowTemplate>
        </Table>

        @if (_plans.Count == 0)
        {
            <div class="text-muted">Geen planningen gevonden.</div>
        }
    </div>
</div>